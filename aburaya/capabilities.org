#+title: Capability Architecture
#+subtitle: Powers, skills, bodies — how spirits act in the world
#+author: mu2tau + mayadev
#+date: 2026-03-13

* The Three-Layer Decomposition

A spirit's ability to act in the world decomposes into three layers.
Each layer has a different owner, a different lifetime, and a different
portability. Conflating them produces confusion; separating them
produces clarity.

| Layer | Owned by | Lives at        | Bound to | Portable?                     |
|-------+----------+-----------------+----------+-------------------------------|
| *Power* | Spirit   | =aburaya/powers/= | Nothing  | Yes — survives harness change |
| *Skill* | Agent    | harness config  | Harness  | No — harness-specific         |
| *Body*  | Host     | host wiring     | Machine  | No — machine-specific         |

** Power (spirit layer)

A power is what a spirit /can do/ — described in natural language,
intelligible to any LLM. Powers live in =aburaya/powers/<name>.md=.
They are harness-independent: a power written for one harness is
equally readable by any other.

Powers are the organisation's shared vocabulary of capability. When
a new spirit is commissioned, its =identity.yaml= lists which powers
it holds. When a composite WP needs a coalition, powers are the
currency of negotiation.

A power definition is a markdown file. It does not contain code.
It describes /what/ and /why/, not /how/ in any specific runtime.

** Skill (agent layer)

A skill is a power /bound to a harness/. The binding translates the
power's natural-language description into the harness's native
format. Common binding patterns include:

| Pattern         | How the power reaches the agent               |
|-----------------+------------------------------------------------|
| Skill file      | A file in the harness's skill directory that   |
|                 | the agent reads at session start               |
| Agent template  | A structured definition (YAML, JSON, etc.)     |
|                 | that includes the power text and tool config   |
| Preamble        | A context file injected into the session as    |
|                 | part of the agent's initial prompt             |
| Inline          | The power text pasted directly into a session  |

The skill layer is where file paths, tool names, and harness-specific
conventions enter. The power says "read the relay"; the skill says
how to do it in this particular runtime — which commands to run,
which directories to look in.

Skills are not portable across harnesses. A skill file written for
one harness is meaningless to another. But the underlying power is
the same — re-binding to a new harness produces a new skill from the
same power. The skill is also where session characteristics are
configured: which tools are available, how context is loaded, and
what the agent can act on.

** Body (host layer)

A body is the sensory and motor wiring that connects a spirit to its
environment. Bodies are machine-specific: event hooks, status
displays, output validators, filesystem watchers.

The body concept emerged from WP-0010 (sutradhar companion). A
companion spirit that persists alongside the human's work needs a
body — hooks that deliver events from the environment, displays that
show state, and validators that check structured output. The body is
what makes the spirit /embodied/ in a particular host.

Not all spirits need bodies. A spirit that runs in a summoned session
(opened, works, closes) has no body — it uses the harness's built-in
tools. A companion spirit needs a body to perceive and act.

| Spirit type | Body needed? | Example                               |
|-------------+--------------+---------------------------------------|
| Summoned    | No           | epistem-guardian executing a survey   |
| Companion   | Yes          | sutradhar-guardian in sidebar         |
| Daemon      | Minimal      | a CI/CD spirit triggered by git hooks |

The body is also responsible for spatial grounding — providing the
spirit with knowledge of where it is in the filesystem, which project
it inhabits, and what paths are available. A harness that does not
inject this context must provide it through a preamble or worksite
prompt.

*** Body binding

Body binding parallels skill binding: a power describes /what/ a body
should do (e.g. "perceive editor events"), and the body implements
/how/ in a specific host environment. The binding is:

| Concern        | Described in       | Implemented in      |
|----------------+--------------------+---------------------|
| What to sense  | Power definition   | Host-specific hooks |
| What to emit   | Power definition   | Host-specific code  |
| Output format  | Power definition   | Validated by body   |

Unlike skill binding, body binding is often one-to-one: a single
host environment has one body implementation per companion spirit.
The binding is not expected to be portable — it is machine-specific
by design.

** Two directions of connection

These three layers map to two communication patterns:

| Direction  | What it connects        | Our layer      |
|------------+-------------------------+----------------|
| Vertical   | Spirit to harness tools | Skill          |
| Horizontal | Spirit to spirit        | Exported power |

Vertical integration means the spirit reaches down through its skill
bindings to use tools (file operations, git, search). Horizontal
collaboration means one spirit uses another spirit's exported power.
Keeping these distinct prevents confusion between "I can use this
tool" (skill) and "another spirit can do this for me" (exported
power).


* Power Definitions

** Format

Power definitions are markdown files in =aburaya/powers/=. They are
written in natural language for LLM consumption. The format is
deliberately lightweight — narrative, not schema.

Every power definition must include:

- *Purpose* — what this power enables (1-3 sentences)
- *Key Principles* — invariants that hold regardless of harness

Beyond these, the structure follows the power's nature. A perception
power (=attend-working-context=) needs sections on what to attend to
and what to ignore. A composition power (=story-compose=) needs voice
guidelines and existing corpus. A protocol power (=relay-protocol=) needs
step-by-step procedures. Let the content dictate the structure.

Avoid rigid templates. The power definitions that exist today evolved
through use — their sections reflect what spirits actually need to
know. Imposing a uniform schema would produce boilerplate where
specificity is needed.

** Recommended sections (use when appropriate)

| Section                      | When to include                                                    |
|------------------------------+--------------------------------------------------------------------|
| Purpose                      | Always                                                             |
| Relationship to Other Powers | When the power is part of a composition (e.g. attend → hold → lay) |
| Key Principles               | Always — the invariants                                            |
| Pairing / Used by            | When multiple spirits share the power                              |
| Constraints / What NOT to do | When the boundary is non-obvious                                   |
| Protocol / How It Works      | When there is a step-by-step procedure                             |

** Authorship

Powers emerge from work, not from planning. A spirit that develops a
reusable capability through practice should extract it as a power
definition. The power's provenance is recorded in =identity.yaml=
(which spirits hold it) and in git history (who wrote it).

** Current inventory

| Power                    | Purpose                                          | Held by                        |
|--------------------------+--------------------------------------------------+--------------------------------|
| =relay-protocol=             | Read and write the sūtra relay                   | All spirits                    |
| =manage-wp-lifecycle=      | Transition WP status and announce in relay       | All spirits                    |
| =author-wp=                | Draft a work package from survey to specification | All spirits                    |
| =author-dispatch-plan=     | Compose a DAG of WPs for coordinated execution   | mayadev                        |
| =orient-to-mayalucia=      | Cold-start orientation to the project            | All spirits (onboarding)       |
| =story-compose=            | Compose fiction set in MāyāLucIA's world         | sutradhar-guardian             |
| =summarize-corpus=         | Distill a body of text into structured summary   | epistem-guardian               |
| =org-content-workflow=     | Org → Hugo content pipeline                      | sutradhar-guardian             |
| =editorial-design=         | Steward a project's visual identity              | cruvin-guardian                |
| =attend-working-context=   | Perceive editor events                           | sutradhar-guardian (companion) |
| =hold-the-thread=          | Maintain continuity across transitions           | sutradhar-guardian (companion) |
| =lay-the-warp=             | Deposit structured understanding as s-expression | sutradhar-guardian (companion) |
| =gaddi=                    | Orchestrate substrate sessions through a task    | mayadev                        |


* Exported Powers

Not all powers are available to other spirits. A spirit's internal
powers are its own business. Exported powers are what it offers as
a service to the organisation.

The =identity.yaml= gains an =exports:= section:

#+begin_src yaml
character:
  powers: [relay-protocol, story-compose, org-content-workflow]
  exports:
    - power: story-compose
      interface: >
        Takes a story seed (setting, theme, characters). Produces
        a markdown story in the MāyāLucIA voice. Requires access
        to the spirit gazetteer for regional vocabulary.
#+end_src

The =exports:= section declares:
- Which powers are available to other spirits
- A brief interface description (inputs, outputs, preconditions)

Powers not listed in =exports:= are internal. =relay-protocol= is a
personal power — every spirit uses it for itself. =story-compose=
is an exported power — the sutradhar offers it to others.


* Skill Injection Protocol

When a composite WP requires powers from multiple spirits, the
executing agent needs access to those powers in its session. This
is skill injection — loading another spirit's power definition
into the current session's context.

** Discovery

The WP names the required powers and their source spirits. The
executing agent reads the WP specification to learn what it needs.

** Loading

How the power definition reaches the agent's context depends on the
agent's autonomy level:

| Mechanism  | Who reads the file | Who provides context           |
|------------+--------------------+--------------------------------|
| Self-read  | The agent itself   | Agent reads from =aburaya/powers/= |
| Shepherded | An orchestrator    | File contents injected into    |
|            |                    | the agent's context window     |
| Manual     | The human          | Human pastes the power text    |
|            |                    | into the session               |

In all three cases, the power definition is the same markdown file.
The loading mechanism is a skill-layer concern — it varies by harness
and by the agent's available tools.

** Grounding

The power is parameterised for the current project context. File
paths, conventions, and constraints from the worksite prompt (the
project-level instruction file) and the WP specification narrow
the power's scope to the task at hand.

** Invocation

The agent uses the injected capability as if it were its own. The
key principle: *the spirit boundary stays clean*. The epistem-guardian
remains an observer even when using aikosh's validation tools. The
injected power is a tool on the bench, not a change of identity.

** What skill injection is NOT

- It is not identity transfer. The executing spirit does not become
  the source spirit.
- It is not runtime discovery. The WP declares what's needed upfront.
  Dynamic discovery is a future consideration.
- It is not tool sharing. The tools available to an agent (file
  operations, search, git) depend on the harness and may vary between
  sessions. Skill injection operates at the power layer — shared
  capability, not shared tools.


* Composite Work Packages and Coalitions

A composite WP declares *tasks*, each requiring specific powers. The
set of spirits whose exported powers are needed across all tasks
forms a *coalition*.

** Coalition lifecycle

The coalition is ephemeral — it forms when the WP enters =executing=
and dissolves when it reaches =completed= or =failed=. The permanent
guardian spirits persist; the coalition is a temporary formation.

** Execution models

A composite WP can execute in several ways:

1. *Single session, multiple concerns*: one agent session loads power
   definitions from multiple spirits and executes all tasks
   sequentially. Works when the session has access to the relevant
   source files.

2. *Sequential sessions*: each task runs in the spirit whose powers
   it needs, handing off artifacts via git. Task A commits its output;
   Task B pulls and continues.

3. *Parallel sessions*: independent tasks run concurrently on
   different hosts. The dispatch message coordinates which host runs
   which task. Artifacts merge via git.

4. *Shepherded execution*: a spirit that lacks direct file access
   receives context through an intermediary (human or orchestrator)
   that reads files into the context window and writes agent output
   to disk. The power stays the same — only the delivery mechanism
   changes. See the autonomy spectrum in the skill section.

** Delegation mechanisms

How a session spawns sub-work depends on the harness:

| Mechanism      | Description                                            |
|----------------+--------------------------------------------------------|
| Sub-session    | The harness spawns an independent agent session that   |
|                | runs a task and returns results to the parent session  |
| Template       | An agent definition references another agent or skill  |
|                | by name; the harness resolves the reference at runtime |
| Handoff        | The current session commits artifacts and signals the  |
|                | next spirit via relay; no sub-session needed           |
| Shepherding    | An orchestrator injects context and harvests output    |
|                | for a substrate that cannot act on its own             |

The specific syntax for sub-sessions and templates varies by harness.
The architectural point is that delegation is a pattern, not a
feature of any one runtime.


* WP Execution Lifecycle

Work packages have a formal lifecycle — a state machine that tracks
a WP from idea through execution to resolution. The state machine is
abstract; how it is encoded in files is a convention concern (see
=develop/work-packages.org= for serialisation details).

#+begin_example
drafted → tightened → executing → completed
                   ↘              ↗
               input-required → resumed
                                  ↘
                               failed
#+end_example

| State          | Meaning                              | Transition by            |
|----------------+--------------------------------------+--------------------------|
| drafted        | WP written, not yet reviewed         | Author (spirit or human) |
| tightened      | Spec reviewed and approved           | Human                    |
| executing      | Agent working on the WP              | Executing spirit         |
| input-required | Agent blocked, needs human input     | Executing spirit         |
| resumed        | Human provided input, work continues | Human                    |
| completed      | Acceptance criteria met              | Executing spirit + human |
| failed         | Cannot complete, reasons documented  | Executing spirit         |

State transitions are announced in the sūtra relay so the
organisation can track progress. The =manage-wp-lifecycle= power
describes how spirits perform transitions in practice.


* Dispatch Messages

A dispatch is a structured assignment of work to a spirit on a host.
It carries a dependency graph of work packages, an execution order,
delegation guidance, and host assignment. The receiving spirit owns
delegation — it decides whether to invoke guild spirits as separate
sessions or execute directly. A dispatch is a suggestion, not a
command. See =develop/work-packages.org= for format conventions.


* Relationship to Existing Documents

** governance.org

=aburaya/governance.org= predates the spirit/guild system. It defines:

- The Measure → Model → Manifest → Evaluate cycle (still valid, still
  the scientific methodology)
- Six role archetypes: Observer, Theorist, Builder, Sculptor, Critic,
  Guide (adopted into =commissioning.org= as archetypes for guardian
  spirits)
- Handoff protocols (superseded by sūtra relay for inter-spirit
  communication)
- Scientific memory architecture (complemented by the bath protocol
  in =kamaji.org= and auto-memory conventions)

The governance document remains the scientific methodology reference.
This document (capabilities.org) handles the organisational mechanics
that governance.org's handoff and memory sections partially addressed.

** commissioning.org

The 10-step commissioning procedure. This document extends it with:

- The three-layer decomposition (power/skill/body) applied at
  commissioning time (established by WP-0008, cruvin-guardian)
- Exported powers in =identity.yaml=
- The coalition model for composite WPs

** kamaji.org

The bath protocol for session restoration. Capabilities.org does not
duplicate it — kamaji.org handles memory; this document handles
capability.

** The powers themselves

=aburaya/powers/*.md= are the primary artifacts. This document
describes the architecture /around/ them — how they compose, how
they're shared, how they relate to skills and bodies. The powers
themselves remain self-contained markdown files.
