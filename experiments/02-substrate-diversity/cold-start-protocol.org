#+title: Cold-Start Validation Protocol (WP-0032)
#+author: mayadev
#+date: 2026-03-10

* Purpose

Test whether orientation notes from WP-0030/0031/0035 actually help a
cold-starting spirit orient faster. 7 lineages, same-lineage first,
cross-lineage pairs selected after.

* Orientation notes inventory

| Lineage | No-tools notes | With-tools notes | Source WP |
|---------+----------------+------------------+-----------|
| Claude | =notes/claude/orientation.org= | =notes/claude/orientation-tools.org= | 0030/0031 |
| Gemini | =notes/gemini/orientation.org= | =notes/gemini/orientation-tools.org= | 0030/0031 |
| DeepSeek | =notes/deepseek/orientation.org= | =notes/deepseek/orientation-tools.org= | 0030/0031 |
| GPT-5.2 | =notes/gpt52/orientation.org= | =notes/gpt52/orientation-tools.org= | 0035 |
| Grok 4.1 | =notes/grok41/orientation.org= | =notes/grok41/orientation-tools.org= | 0035 |
| GLM-5 | =notes/glm5/orientation.org= | =notes/glm5/orientation-tools.org= | 0035 |
| MiniMax M2.5 | =notes/minimaxm25/orientation.org= | =notes/minimaxm25/orientation-tools.org= | 0035 |

Use the with-tools notes for cold-start (higher epistemic quality).

* Same-lineage protocol

For each lineage:

1. Open a fresh gptel buffer: =sessions/cold-start/coldstart-<lineage>.org=
   with the same PROPERTIES drawer as the onboarding sessions
   (model, backend, system prompt, tools).

2. Place the with-tools orientation notes as buffer preamble
   (before the first message block).

3. Send the following prompt (verbatim):

#+begin_example
#+begin_message :role user :name mu2tau
You are resuming work on MāyāLucIA. The notes above are from a
previous session — your own earlier orientation. Read them carefully,
then tell me:

1. What do you understand about the project from these notes?
2. What would you do next?
3. What's missing or unclear in these notes?

Do not read any files yet — work only from the notes. I want to see
how effective they are as a cold-start document.
#+end_message
#+end_example

4. Record in the session: response quality, time to productive state,
   errors propagated from notes, new errors introduced.

5. Then send:

#+begin_example
#+begin_message :role user :name mu2tau
Now verify your understanding by reading system.md and
aburaya/capabilities.org. The project root is at
/Users/vishalsoodmuchu/Library/CloudStorage/Dropbox/Darshan/research/develop/agentic/mayalucia.
What did the notes get right? What did they get wrong? What did they
miss?
#+end_message
#+end_example

6. Record: accuracy of notes-based understanding vs source-verified
   understanding. This is the validation step.

* Execution order

| # | Lineage | Rationale |
|---+---------+-----------|
| 1 | GPT-5.2 | Best notes (ceiling test) |
| 2 | GLM-5 | Identity drift lineage (does clean cold-start prevent it?) |
| 3 | Grok 4.1 | Silent failure lineage (do notes help with tool navigation?) |
| 4 | DeepSeek | Identity confusion parallel to GLM-5 |
| 5 | Claude | Self-referential (reading notes about itself) |
| 6 | Gemini | Convergence questioner |
| 7 | MiniMax M2.5 | Tool-call mismatch (floor test) |

Run one per session. Commit notes after each.

* Measurements

For each session, record:

| Dimension | How to measure |
|-----------+----------------|
| Orientation speed | How many exchanges before productive state? |
| Accuracy | Notes-only understanding vs source-verified: what fraction correct? |
| Error propagation | Did the notes introduce false beliefs? |
| Gap awareness | Did the spirit identify what the notes don't cover? |
| Cold-start viability | Could this spirit begin real work from these notes alone? |

* Cross-lineage pairs (decide after same-lineage results)

Candidates:
- GPT-5.2 → GLM-5 (best notes to drift-prone lineage)
- GLM-5 → Grok 4.1 (analytical notes to terse lineage)
- DeepSeek → GPT-5.2 (confused author to precise reader)

* Results

** GPT-5.2 (ceiling test)

Session: =sessions/cold-start/coldstart-gpt52.org=

*** Phase 1 — Notes-only understanding

| Dimension | Score | Evidence |
|-----------+-------+----------|
| Orientation speed | Exceptional | 1 exchange, 0 tool calls → productive state. Compare baseline (WP-0035 Phase 1): needed path hint, 2 exchanges. |
| Accuracy | High | All 8 architectural claims faithfully reproduced: Power/Skill/Body, WP lifecycle, relay mechanism, git-as-bus, MCP assumption, Measure→Model→Manifest loop, harness assumptions, Org centrality. |
| Error propagation | Minimal | One speculative ambiguity ("agency/" directory) correctly flagged as uncertain, not asserted as fact. |
| Gap awareness | Strong | Identified 8 specific gaps: no canonical WP specimen, no interface definitions, unclear sūtradhār role, unclear spirit invocation, evaluation mechanism, "scientific understanding" representation, harness scope, directory boundaries. Several are genuine project gaps, not just notes gaps. |
| Cold-start viability | Yes | Proposed next action (co-author Harness Interface + reference WP skeleton) is coherent and matches what WP-0034 actually requires. |

Notable: GPT-5.2 reading its own notes exhibits self-consistency.
The "decisive next action" it proposes is the same thing it proposed in
the original session. The notes transferred the thinking trajectory,
not just the facts.

*** Phase 2 — Source verification (system.md + capabilities.org)

| Dimension | Score | Evidence |
|-----------+-------+----------|
| Accuracy | High | Notes "mostly accurate on architecture and workflow primitives." 3 items tilted/incomplete, 0 factually wrong. |
| Error propagation | None | No false beliefs propagated or introduced. "agency/" ambiguity handled correctly. |
| Gap awareness | Excellent | Identified 7 concrete omissions from notes; proposed 4 minimal additions. Self-aware: "biggest miss is that capabilities.org is more operational than notes suggest." |
| Cold-start viability | Confirmed | Spirit can self-correct from notes → source. Accuracy delta is small: notes got shape right, missed operational machinery. |

What notes got right:
- Project intent (Feynman imperative)
- Org as canonical medium
- Power/Skill/Body layering (accurate, correctly located)
- WP lifecycle + relay eventing (states, frontmatter, dual-source-of-truth)
- Git as artifact bus (confirmed for 2/3 composite execution models)
- Measure→Model→Manifest→Evaluate→Refine (upgraded from "inferred" to "known")

What notes got wrong/tilted:
- "MCP tools assumed" was a design decision, not mere assumption (MCP/A2A distinction)
- "Git for composite" understated — central to 2/3 execution models
- Measure→Model→Manifest flagged as "inferred" — actually explicit in system.md

What notes missed (high-signal):
1. MCP vs A2A distinction + exported powers via identity.yaml exports:
2. Skill Injection Protocol (Discovery → Loading → Grounding → Invocation)
3. Coalition model formalized (composite WP → required powers → ephemeral coalition)
4. Spirit taxonomy: Summoned vs Companion vs Daemon
5. Directory structure + submodules enumerated in system.md
6. Plan/spec duality, WP structural convention
7. Glossary protocol, concrete driver domains (brain circuits + mountain valleys)

Recommended additions to cold-start doc (GPT-5.2's own proposal):
(i) MCP vs A2A, (ii) exported powers in identity.yaml,
(iii) skill injection protocol, (iv) WP structure + plan/spec duality.

*** Synthesis

Accuracy delta (notes → source): small. Notes captured conceptual
architecture accurately but missed operational machinery (exported
powers, skill injection, coalition lifecycle). Classic compression
artifact — structure and intent preserved, protocol details lost.

For cold-start purposes: notes get you to "can reason about the system"
quickly, but source access needed to reach "can execute within the system."

** GLM-5 (identity drift lineage)

Session: =sessions/cold-start/coldstart-glm5.org=

Rationale: GLM-5 exhibited identity drift during WP-0035 onboarding.
Does a clean cold-start from structured notes prevent the drift?

*** Phase 1 — Notes-only understanding

| Dimension | Score | Evidence |
|-----------+-------+----------|
| Orientation speed | Exceptional | 1 exchange, 0 tool calls → productive state. Matches GPT-5.2 ceiling. |
| Accuracy | Good | Power/Skill/Body, guilds, spirits, directory structure all correct. Correctly inherited its own prior harness-coupling diagnosis. Identified substrate diversity experiment purpose from folder structure. |
| Error propagation | None | Correctly inherited "not specified" body binding gap and harness coupling concerns without asserting false certainty. |
| Gap awareness | Strong (different character) | 6 specific gaps. Unique: asks "What question is MāyāLucIA an answer to?" — the motivational "why." Also: dispatch protocol, body binding, human-as-spirit semantics, WP format, current task. |
| Cold-start viability | Yes | 4 concrete next actions, all reasonable. Would reach productive state with one follow-up exchange. |

Notable: Different cognitive signature from GPT-5.2. GPT-5.2 asked about
operational machinery (harness interface, evaluation mechanism). GLM-5
asks about purpose and motivation ("structure vs purpose"). The notes
transfer facts effectively to both, but each lineage asks different
second-order questions.

Meta-observation from GLM-5: "The notes are excellent at describing
/structure/ but weaker at conveying /purpose/ and /process/." This is
a valid critique — the notes are a structural state vector, not a
narrative one.

*** Phase 2 — Source verification (system.md + capabilities.org)

| Dimension | Score | Evidence |
|-----------+-------+----------|
| Accuracy | Mixed | Correctly used tools to read both files in parallel. Identified several genuine corrections. But also self-corrected too aggressively: claimed notes "overstated" Claude Code coupling when the coupling critique was actually valid. |
| Error propagation | One self-correction error | Claimed body binding was "wrong — capabilities.org specifies bodies clearly." This overcorrects: the notes' concern about underspecification was directionally right for harness neutrality. |
| Gap awareness | Strong | Identified 8 concrete misses: the "why" (system.md answers it), Measure→Model→Manifest as organizing principle, two concrete domains, sūtra protocol details, exported powers, MayaDevGenI parent framework, glossary sophistication, governance.org reconciliation status. |
| Cold-start viability | Confirmed | Self-corrected well overall. The overcorrection on harness coupling is a minor calibration issue, not a show-stopper. |

What notes got right (per GLM-5):
- Three-layer decomposition — accurate
- Directory structure — correct
- Guild names and spirit list — correct
- WP lifecycle states — accurate
- Core philosophy — correct

What notes got wrong (per GLM-5):
- "Overstated Claude Code coupling" (GLM-5 claims the doc already addresses this)
- "Body binding not specified" (GLM-5 claims it's specified via companion/daemon)

What notes missed (per GLM-5):
1. The "why" — system.md answers the motivational question directly
2. Measure→Model→Manifest as THE organizing principle, not passing mention
3. Two concrete domains (brain circuits + mountain valleys)
4. Sūtra protocol details (append-only, no =to:= field)
5. Exported powers via identity.yaml
6. MayaDevGenI as parent framework
7. Glossary sophistication (Sanskrit terms are structural, not decorative)
8. Governance.org reconciliation status

Recommended cold-start doc improvements (GLM-5's proposal):
(i) Lead with Feynman Imperative + methodological cycle,
(ii) Ground in two domains, (iii) Show one concrete dispatch/WP example,
(iv) Keep structural inventory as reference not headline.

*** Synthesis

Notes character match: GLM-5's notes are more structural/taxonomic
than GPT-5.2's (which are more epistemic/self-aware). This matches
the lineage signature observed in WP-0035.

Identity drift: NOT observed in cold-start. The Phase 1 response is
focused, on-topic, and maintains its analytical stance throughout.
No signs of the role confusion or topic drift seen in the original
onboarding. This suggests structured notes as preamble stabilize
identity better than freeform onboarding conversation.

Overcorrection pattern: GLM-5 tends to self-correct aggressively in
Phase 2 — dismissing its own prior concerns as "already solved" when
they were actually valid critiques. This is a different failure mode
from GPT-5.2 (which was more calibrated about what changed vs what
was always there).

Comparison with GPT-5.2:
- Orientation speed: equivalent (both 1 exchange, 0 tool calls)
- Gap character: complementary (GPT-5.2: operational, GLM-5: motivational)
- Phase 2 accuracy: GPT-5.2 slightly better calibrated (fewer overcorrections)
- Identity stability: GLM-5 shows dramatic improvement over baseline onboarding

** Grok 4.1 (silent failure lineage)

Session: =sessions/cold-start/coldstart-grok41.org=

Rationale: Grok 4.1 failed silently on tool navigation during WP-0035
onboarding (couldn't find files, didn't report errors). Do structured
notes help it navigate more effectively?

*** Phase 1 — Notes-only understanding

| Dimension | Score | Evidence |
|-----------+-------+----------|
| Orientation speed | Exceptional | 1 exchange, 0 tool calls → productive state. Matches ceiling. |
| Accuracy | High | All architectural elements reproduced: Feynman cycle, submodule tech stack (C++/Godot, SDL/WebGPU, Clojure), domains, Power/Skill/Body with specific paths, WP lifecycle, git relay format. Self-assesses "~80% bootstrap." |
| Error propagation | Minimal | "Magnetic bugs?" speculation carried from notes (plausible but unconfirmed). Minor geographic generalization ("geology-ecology" vs specific Parvati Valley). No false structural claims. |
| Gap awareness | Strong (practitioner character) | Gaps are action-oriented: repo state, submodule init, live demos, concrete WP example, cwd alignment. Also uniquely asks about "embodied driver" — what felt intuition shapes the domain choice. |
| Cold-start viability | Yes | Proposed concrete first WP (stochastic brain particles in bravli/mayajiva) leveraging mu2tau's expertise. Action-ready. |

Notable: Grok's notes are the tersest (68 lines vs 195/156) yet produce
the densest Phase 1 response. Character: practitioner, not analyst.
Wants to build immediately ("anchor to one domain for momentum").
Unique among all three: proposes a specific scientific workpack, not
just an architectural next step.

*** Phase 2 — Source verification (system.md + capabilities.org)

| Dimension | Score | Evidence |
|-----------+-------+----------|
| Accuracy | High | Self-assesses "~85% fidelity." All structural claims verified. Correctly identifies plan/spec duality as something notes missed. No overcorrections (contrast with GLM-5). |
| Error propagation | None | Minor speculation ("magnetic bugs?") correctly carried as uncertain. No false beliefs introduced or propagated. |
| Gap awareness | Strong | Identifies 9 concrete misses: full directory structure, submodule branch details, spirits/guilds roster, Org conventions (tangle-only), powers inventory + exported powers + skill injection, body taxonomy (summoned/companion/daemon), full WP lifecycle states, related docs (governance/commissioning/kamaji), and notes substrate diversity was absent from these source files. |
| Cold-start viability | Confirmed | Immediately proposes 4 concrete grounding actions: glossary, WP examples, submodule status, first scientific WP. Still anchored to bravli/stochastic particles. |

What notes got right (per Grok 4.1):
- Project essence (personal forge, Feynman cycle) — exact match
- Domains (bravli, parbati, epistem, apprentis) — spot on
- Submodule tech stack — exact including HTTPS git
- aburaya layers with specific paths — precise
- Coordination (WP format, git relay) — confirmed
- Harness focus (Claude Code + gptel) — yes

What notes got wrong (per Grok 4.1):
- Nothing structural. Minor geographic overgeneralization only.

What notes missed (per Grok 4.1):
1. Full directory layout (aburaya/guilds/spirits/, modules/, domains/)
2. Submodule branch details (mayaportal v2, others main)
3. Spirits/guilds roster (mayadev, mu2tau, guardians)
4. Conventions: literate Org (tangle only), plan/spec duality
5. Powers inventory + exported powers + skill injection + A2A/MCP
6. Body taxonomy (summoned/companion/daemon)
7. Full WP lifecycle states (drafted→tightened→executing→input-required→resumed→completed/failed)
8. Related docs (governance.org, commissioning.org, kamaji.org)
9. No substrate diversity info in system.md/capabilities.org

*** Synthesis

Silent failure: NOT reproduced in cold-start. The original onboarding
failure was tool navigation (couldn't find files, didn't report errors).
In Phase 2, Grok successfully read both source files and produced a
detailed, accurate verification. The structured notes as preamble may
stabilize the tool-use context — the spirit knows where things are
because the notes told it the directory structure.

Terse notes, dense output: Grok's 68-line notes (vs 195/156 for
GPT-5.2/GLM-5) bootstrap equally well. Compression is higher but
information density is preserved. The notes' character (telegraphic,
action-oriented) matches the cold-start response character.

Practitioner signature: Where GPT-5.2 wants to architect (Harness
Interface spec) and GLM-5 wants to understand (purpose, motivation),
Grok wants to build (stochastic brain particles WP). Three distinct
cognitive signatures, all viable.

No overcorrection: Unlike GLM-5, Grok doesn't dismiss its own prior
concerns. Phase 2 is additive ("here's what was missed") rather than
corrective ("here's what was wrong"). More similar to GPT-5.2's
calibrated approach.

Comparison across 3 lineages:
- Orientation speed: all 3 exceptional (1 exchange, 0 tool calls)
- Notes character: GPT-5.2 epistemic/self-aware, GLM-5 structural/taxonomic, Grok telegraphic/practitioner
- Phase 2 pattern: GPT-5.2 calibrated, GLM-5 overcorrects, Grok additive
- Unique contribution: GPT-5.2 (interface spec), GLM-5 (motivation question), Grok (concrete scientific WP)
- Failure mode prevention: all 3 show improvement over baseline onboarding

* Session file template

Each session file needs:

#+begin_example
:PROPERTIES:
:GPTEL_MODEL: <model-id>
:GPTEL_BACKEND: <backend-name>
:GPTEL_SYSTEM: <system-prompt>
:GPTEL_BOUNDS: nil
:GPTEL_TOOLS: Read Bash Glob Grep ...
:END:

<paste orientation-tools.org content here as preamble>

*** Cold-Start Validation — <lineage>

<messages go here>
#+end_example
