# Build artifacts
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.dblite
*.os
*.o
*.so
*.dylib

# Agent session transcripts (large, ephemeral)
.agent-shell/

# Claude Code local config
.claude/

# SCons
.sconsign.dblite

# Editor/OS
*~
\#*\#
.DS_Store

# Duplicate CLAUDE files (org is source of truth)
CLAUDE-comprehensive.md
CLAUDE-root-only.md

# Python
__pycache__/
*.pyc

# LaTeX generated (keep .org and .tex, not build output)
*.pdf
*.fdb_latexmk
*.fls

# ox-hugo LaTeX intermediates (org is source of truth)
website/ox-hugo-export.tex

# Collab local artifacts
collab/.local/*.pdf
collab/.local/*.html

# OpenCode agent sessions (local)
.opencode/

# Deployment staging area (Hugo build output)
deployment/.staging/

# Hugo generated site (rebuild from source)
website/public/
website/.hugo_build.lock

# Attic — archived artifacts for later review
.attic/

# Experiments — local scratch work
experiments/

# Sūtra relay clone (per-project harness, local cursor)
.sutra/

sutradhar/.cpcache/
